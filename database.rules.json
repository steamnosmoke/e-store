{
  "rules": {
    ".read": true,
    ".write": true,
      "products": {
      ".indexOn": ["category", "price", "memory", "brand","rating", "name", "id"]
    },
      "categories":{
        ".indexOn":["name"]
      }
  }
}